<%= simple_form_for [:admin, @post] do |f| %>
  <%= f.input :title %>
  <%= f.input :body, input_html: { class: 'wysiwyg' } %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Back', admin_posts_path %>

